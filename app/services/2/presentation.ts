export function formatPhase(phase: string) {
  if (phase === 'DEFAULT') {
    return 'Default';
  }
  return phase;
}

export function formatPart(part: string) {
  if (part === 'DEFAULT') {
    return 'Default';
  }
  return part;
}
